# ⚡ Replit Quick Start - Letters App

**Get your app running in under 5 minutes!**

---

## 🚀 One-Command Setup

```bash
bash setup-replit.sh
```

That's it! The script handles everything.

---

## 🔐 Required Secrets

Add these in **Replit Secrets** (🔒 tab):

| Secret | Value | How to Get |
|--------|-------|------------|
| `DATABASE_URL` | `file:./dev.db` | Use SQLite (default) |
| `NEXTAUTH_URL` | `https://your-repl.replit.dev` | Your Repl URL |
| `NEXTAUTH_SECRET` | Random string | Auto-generated by script |

**Quick Setup:**
1. Click 🔒 **Secrets** tab
2. Click **"New secret"**
3. Add each variable above
4. Click **"Add secret"**

---

## ▶️ Run Commands

```bash
# Development mode (with hot reload)
npm run dev

# Production build
npm run build

# Production server
npm start

# Database management
npx prisma studio    # Open database GUI
npx prisma migrate   # Run migrations
```

---

## 🌐 Access Your App

**Development:** `https://your-repl-name.yourusername.repl.co`

**Production:** Same URL (when Always On is enabled)

---

## 📝 Common Commands

```bash
# Install dependencies
npm install

# Generate Prisma Client
npx prisma generate

# Run database migrations
npx prisma migrate dev

# Build for production
npm run build

# Start production server
npm start

# View database (Prisma Studio)
npx prisma studio

# Check for errors
npm run lint
npx tsc --noEmit
```

---

## 🆘 Quick Fixes

| Problem | Solution |
|---------|----------|
| **Port conflicts** | Replit handles ports automatically |
| **Database errors** | Check `DATABASE_URL` in Secrets |
| **Build fails** | `rm -rf .next && npm run build` |
| **Prisma errors** | `npx prisma generate` |
| **Module not found** | `rm -rf node_modules && npm install` |
| **Env vars not loading** | Check Secrets tab, restart Repl |

---

## 📚 Full Documentation

- **Complete Guide**: [REPLIT_DEPLOYMENT.md](./REPLIT_DEPLOYMENT.md)
- **Setup Script**: [setup-replit.sh](./setup-replit.sh)
- **Main README**: [README.md](./README.md)

---

## ✅ Checklist

- [ ] Run `bash setup-replit.sh`
- [ ] Configure Secrets (🔒 tab)
- [ ] Run `npm run dev`
- [ ] Test your app URL
- [ ] Verify database connection
- [ ] Check health endpoint: `/api/health`

---

**Need help?** Check [REPLIT_DEPLOYMENT.md](./REPLIT_DEPLOYMENT.md) for detailed troubleshooting.

